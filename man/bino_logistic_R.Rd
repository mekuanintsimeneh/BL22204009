% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code.R
\name{bino_logistic_R}
\alias{bino_logistic_R}
\title{Binomial Logistic Regression using Gradient Descent}
\usage{
bino_logistic_R(X, y, alpha = 0.01, max_iter = 1000, tol = 1e-06)
}
\arguments{
\item{X}{A numeric matrix of predictors.}

\item{y}{A numeric vector of binary outcomes.}

\item{alpha}{A numeric value for the learning rate.}

\item{max_iter}{Maximum number of iterations for gradient descent.}

\item{tol}{Convergence tolerance.}
}
\value{
A numeric vector of estimated coefficients.
}
\description{
This function fits a logistic regression model using gradient descent.
}
\examples{
set.seed(123)
X <- matrix(rnorm(100 * 3), ncol = 3)  # Random predictors
beta_true <- c(0.5, -1, 2)  # True coefficients
y <- rbinom(100, 1, prob = logistic_R(X \%*\% beta_true))  # Generate binary outcome
beta_est <- bino_logistic_R(X, y, alpha = 0.01, max_iter = 500, tol = 1e-6)
print(beta_est)
}
